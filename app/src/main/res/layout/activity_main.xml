<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/gel_bg">

    <!-- TOOLBAR -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/gelToolbar"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:background="#000000"
        android:elevation="8dp"
        android:titleTextColor="#FFD700"/>

    <!-- SCROLL CONTENT (ScrollView MUST have exactly 1 direct child) -->
    <ScrollView
        android:id="@+id/scrollRoot"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true">

        <!-- ✅ THE ONLY DIRECT CHILD -->
        <LinearLayout
            android:id="@+id/contentRoot"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="12dp"
            android:gravity="center_horizontal">

            <!-- ================================================= -->
            <!-- HEADER -->
            <!-- ================================================= -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:paddingTop="6dp"
                android:paddingBottom="6dp">

                <ImageView
                    android:id="@+id/imgLogo"
                    android:layout_width="80dp"
                    android:layout_height="80dp"
                    android:src="@drawable/gel_logo"
                    android:contentDescription="@string/app_name"
                    android:layout_marginEnd="6dp"/>

                <TextView
                    android:id="@+id/txtTitle"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:gravity="center"
                    android:text="@string/app_name"
                    android:textColor="#00FFC8"
                    android:textStyle="bold"
                    android:textSize="22sp"
                    android:singleLine="true"/>

                <!-- LANG SWITCHER — VERTICAL (inside header row) -->
                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:gravity="end"
                    android:layout_marginStart="8dp">

                    <TextView
                        android:id="@+id/btnLangGR"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="GR 🇬🇷"
                        android:textSize="16sp"
                        android:textColor="@color/gel_text_primary"
                        android:paddingBottom="6dp"/>

                    <TextView
                        android:id="@+id/btnLangEN"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="EN 🇬🇧"
                        android:textSize="16sp"
                        android:textColor="@color/gel_text_primary"/>
                </LinearLayout>

            </LinearLayout>

            <!-- ================================================= -->
            <!-- TOP ACTIONS -->
            <!-- ================================================= -->

            <!-- DONATE -->
            <Button
                android:id="@+id/btnDonate"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/donate"
                android:background="@drawable/gel_btn_danger_outline"
                android:textColor="@color/gel_btn_text_white"
                android:layout_marginBottom="14dp"
                android:singleLine="true"/>

            <!-- DEVICE DECLARATION -->
            <Button
                android:id="@+id/btnAppleDeviceDeclaration"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:text="@string/apple_device_declaration"
                android:background="@drawable/gel_btn_outline_selector"
                android:textColor="@color/gel_btn_text_white"
                android:gravity="center"
                android:maxLines="2"
                android:padding="6dp"
                android:layout_marginBottom="18dp"/>

            <!-- ================================================= -->
            <!-- SYSTEM -->
            <!-- ================================================= -->
            <TextView
                android:id="@+id/section_system"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/system"
                android:textColor="@color/gel_text_primary"
                android:textStyle="bold"
                android:textSize="20sp"
                android:paddingLeft="6dp"
                android:layout_marginTop="6dp"
                android:layout_marginBottom="6dp"/>

            <!-- PHONE INFO ROW -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginBottom="12dp">

                <Button
                    android:id="@+id/btnPhoneInfoInternal"
                    android:layout_width="0dp"
                    android:layout_weight="1"
                    android:layout_height="60dp"
                    android:text="@string/phone_info_internal"
                    android:background="@drawable/gel_btn_outline_selector"
                    android:textColor="@color/gel_btn_text_white"
                    android:gravity="center"
                    android:maxLines="2"
                    android:padding="6dp"
                    android:layout_marginEnd="6dp"/>

                <Button
                    android:id="@+id/btnPhoneInfoPeripherals"
                    android:layout_width="0dp"
                    android:layout_weight="1"
                    android:layout_height="60dp"
                    android:text="@string/phone_info_peripherals"
                    android:background="@drawable/gel_btn_outline_selector"
                    android:textColor="@color/gel_btn_text_white"
                    android:gravity="center"
                    android:maxLines="2"
                    android:padding="6dp"/>
            </LinearLayout>

            <!-- CPU/RAM LIVE -->
            <Button
                android:id="@+id/btnCpuRamLive"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:text="@string/cpu_ram_live"
                android:background="@drawable/gel_btn_outline_selector"
                android:textColor="@color/gel_btn_text_white"
                android:layout_marginBottom="16dp"
                android:singleLine="true"
                android:ellipsize="end"/>

            <!-- ================================================= -->
            <!-- CLEANER -->
            <!-- ================================================= -->
            <TextView
                android:id="@+id/section_clean"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/cleaner"
                android:textStyle="bold"
                android:textSize="20sp"
                android:textColor="@color/gel_text_primary"
                android:paddingLeft="6dp"
                android:layout_marginTop="12dp"
                android:layout_marginBottom="6dp"/>

            <!-- CLEAN ALL -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center">

                <LinearLayout
                    android:id="@+id/btnCleanAll"
                    android:layout_width="match_parent"
                    android:layout_height="60dp"
                    android:background="@drawable/gel_btn_danger_outline"
                    android:gravity="center"
                    android:orientation="horizontal"
                    android:padding="12dp"
                    android:layout_margin="4dp">

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@string/clean_all"
                        android:textColor="@color/white"
                        android:textStyle="bold"
                        android:textSize="17sp"/>
                </LinearLayout>

            </LinearLayout>

            <!-- ================================================= -->
            <!-- JUNK -->
            <!-- ================================================= -->
            <TextView
                android:id="@+id/section_junk"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/junk"
                android:textStyle="bold"
                android:textSize="20sp"
                android:textColor="@color/gel_text_primary"
                android:layout_marginTop="18dp"
                android:paddingLeft="6dp"/>

            <!-- CACHE BUTTONS -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

                <Button
                    android:id="@+id/btnBrowserCache"
                    android:layout_width="0dp"
                    android:layout_weight="1"
                    android:layout_height="60dp"
                    android:text="@string/browser_cache"
                    android:background="@drawable/gel_btn_outline_selector"
                    android:textColor="@color/gel_btn_text_white"
                    android:layout_margin="4dp"
                    android:gravity="center"
                    android:maxLines="2"
                    android:padding="6dp"/>

                <Button
                    android:id="@+id/btnAppCache"
                    android:layout_width="0dp"
                    android:layout_weight="1"
                    android:layout_height="60dp"
                    android:text="@string/app_cache_btn"
                    android:background="@drawable/gel_btn_outline_selector"
                    android:textColor="@color/gel_btn_text_white"
                    android:layout_margin="4dp"
                    android:gravity="center"
                    android:maxLines="2"
                    android:padding="6dp"/>
            </LinearLayout>

            <!-- ================================================= -->
            <!-- PERFORMANCE -->
            <!-- ================================================= -->
            <TextView
                android:id="@+id/section_performance"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/performance"
                android:textStyle="bold"
                android:textSize="20sp"
                android:textColor="@color/gel_text_primary"
                android:layout_marginTop="18dp"
                android:paddingLeft="6dp"/>

            <!-- PERFORMANCE TOOLS ROW -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginTop="12dp">

                <Button
                    android:id="@+id/btnAppManager"
                    android:layout_width="0dp"
                    android:layout_weight="1"
                    android:layout_height="60dp"
                    android:text="@string/app_manager_btn"
                    android:background="@drawable/gel_btn_outline_selector"
                    android:textColor="@color/gel_btn_text_white"
                    android:layout_margin="4dp"
                    android:gravity="center"
                    android:maxLines="2"
                    android:padding="6dp"/>

                <Button
                    android:id="@+id/btnGuidedOptimizer"
                    android:layout_width="0dp"
                    android:layout_weight="1"
                    android:layout_height="60dp"
                    android:text="@string/guided_optimizer_btn"
                    android:textAllCaps="false"
                    android:textColor="#00FF7F"
                    android:background="@drawable/gel_btn_neon_outline"
                    android:layout_margin="4dp"
                    android:gravity="center"
                    android:maxLines="2"
                    android:padding="6dp"/>

            </LinearLayout>

            <!-- ================================================= -->
            <!-- DIAGNOSIS -->
            <!-- ================================================= -->
            <Button
                android:id="@+id/btnDiagnostics"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:text="@string/diagnostics"
                android:background="@drawable/gel_btn_outline_selector"
                android:textColor="#00FFBF"
                android:textStyle="bold"
                android:textSize="22sp"
                android:layout_marginTop="28dp"
                android:layout_marginBottom="20dp"
                android:singleLine="true"
                android:ellipsize="end"
                android:drawableLeft="@drawable/medical"
                android:drawablePadding="2dp"
                android:gravity="center"/>

            <!-- RETURN TO ANDROID MODE (CLEAN ASCII) -->
            <Button
                android:id="@+id/btnReturnAndroid"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:text="RETURN TO ANDROID MODE"
                android:textAllCaps="false"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="#FFFFFF"
                android:background="@drawable/gel_btn_outline_selector"
                android:layout_marginTop="10dp"
                android:layout_marginBottom="12dp"/>

<!-- ================================================= -->
<!-- APPLE DIAGNOSTIC INFO -->
<!-- ================================================= -->

<LinearLayout
    android:id="@+id/appleInfoPanel"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="14dp"
    android:layout_marginTop="10dp"
    android:layout_marginBottom="12dp"
    android:background="@drawable/gel_panel_gold">

    <TextView
        android:id="@+id/txtAppleInfo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="#00FF9C"
        android:textSize="14sp"
        android:lineSpacingMultiplier="1.2"/>

</LinearLayout>

<!-- ================================================= -->
<!-- APPLE INFO — FIXED CONTINUE BUTTON -->
<!-- ================================================= -->
<Button
    android:id="@+id/btnAppleInfoContinue"
    android:layout_width="match_parent"
    android:layout_height="60dp"
    android:text="OK"
    android:textAllCaps="false"
    android:textColor="#00FF7F"
    android:textStyle="bold"
    android:textSize="22sp"
    android:background="@drawable/gel_btn_neon_outline"
    android:gravity="center"
    android:layout_marginTop="8dp"
    android:layout_marginBottom="20dp"/>

</LinearLayout>
</ScrollView>
</LinearLayout>
